<%= form_for(aaa) do |f| %>
  <% if aaa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(aaa.errors.count, "error") %> prohibited this aaa from being saved:</h2>

      <ul>
      <% aaa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
